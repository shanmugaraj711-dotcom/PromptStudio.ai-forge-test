import { useState } from "react";
import { useAuth } from "../../context/AuthContext";
import { createApkForgeBrief, payForApkForge } from "../../services/apkForge";

const inspectFile = async (file) => { const head = new Uint8Array(await file.slice(0, 2).arrayBuffer()); const signature = head.length === 2 ? String.fromCharCode(head[0], head[1]) : ""; const executable = signature === "MZ" || /\.(exe|dll|msi|com|scr|bin)$/i.test(file.name); return { name: file.name, size: file.size, mimeType: file.type || "unknown", signature, detectedType: executable ? "executable_or_binary" : "reference" }; };
export default function ApkForge() {
  const { user } = useAuth(); const [description,setDescription]=useState(""); const [websiteUrl,setWebsiteUrl]=useState(""); const [references,setReferences]=useState([]); const [result,setResult]=useState(null); const [error,setError]=useState(""); const [busy,setBusy]=useState(false);
  const addFiles=async(e)=>{setReferences(await Promise.all(Array.from(e.target.files||[]).slice(0,8).map(inspectFile)));setError("");};
  const submit=async(e)=>{e.preventDefault();setBusy(true);setError("");setResult(null);try{const idToken=await user.getIdToken();const request=await createApkForgeBrief({description,websiteUrl,idToken,references});setResult(request);await payForApkForge({user,forgeRequestId:request.forgeRequestId,idToken,description:"PromptStudio AI — APK Forge build",onSuccess:(payment)=>setResult(c=>({...c,...payment}))});}catch(cause){setError(cause.message||"Forge could not start checkout.");}finally{setBusy(false);}};
  return <main className="min-h-[calc(100vh-5rem)] bg-slate-50 px-4 py-8 text-slate-900 sm:px-6"><div className="mx-auto max-w-5xl"><div className="mb-8"><span className="inline-flex rounded-full border border-indigo-200 bg-indigo-50 px-3 py-1 text-xs font-bold uppercase tracking-wider text-indigo-700">APK Forge</span><h1 className="mt-3 text-3xl font-black tracking-tight sm:text-4xl">Turn your existing product into an Android build.</h1><p className="mt-3 max-w-3xl text-slate-600">Describe the product, add evidence, then pay securely. Every paid request enters human review before an isolated Android build starts.</p></div><form onSubmit={submit} className="grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><label className="text-sm font-bold text-slate-800" htmlFor="forge-description">What should the app do?</label><textarea id="forge-description" value={description} onChange={e=>setDescription(e.target.value)} required maxLength={12000} rows={10} className="mt-3 w-full resize-y rounded-2xl border border-slate-200 bg-slate-50 p-4 text-sm outline-none focus:border-indigo-400 focus:ring-4 focus:ring-indigo-100" placeholder="Example: Recreate my desktop app as a modern Android app with login, dashboard, search and export."/><label className="mt-5 block text-sm font-bold text-slate-800" htmlFor="forge-website">Existing website (optional)</label><input id="forge-website" type="url" value={websiteUrl} onChange={e=>setWebsiteUrl(e.target.value)} placeholder="https://example.com" className="mt-3 w-full rounded-2xl border border-slate-200 bg-slate-50 p-3.5 text-sm outline-none focus:border-indigo-400 focus:ring-4 focus:ring-indigo-100"/><label className="mt-5 block text-sm font-bold text-slate-800" htmlFor="forge-files">Reference files</label><input id="forge-files" type="file" multiple onChange={addFiles} className="mt-3 block w-full rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-4 text-sm"/><p className="mt-2 text-xs text-slate-500">Up to 8 references, 25 MB each. Executables are inert evidence and never executed.</p>{references.length>0&&<div className="mt-4 space-y-2">{references.map(file=><div key={`${file.name}-${file.size}`} className="flex items-center justify-between rounded-xl bg-slate-50 px-3 py-2 text-xs"><span className="truncate font-semibold">{file.name}</span><span className={file.detectedType==="executable_or_binary"?"font-bold text-amber-700":"text-slate-500"}>{file.detectedType}</span></div>)}</div>}{error&&<div className="mt-5 rounded-2xl border border-red-200 bg-red-50 p-3 text-sm text-red-700" role="alert">{error}</div>}<button type="submit" disabled={busy||!description.trim()} className="mt-6 w-full rounded-2xl bg-slate-900 px-5 py-3.5 text-sm font-bold text-white transition hover:bg-slate-800 disabled:cursor-not-allowed disabled:opacity-50">{busy?"Preparing secure checkout…":"Continue to secure payment →"}</button></section><aside className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><h2 className="text-lg font-black">Forge pipeline</h2><div className="mt-5 space-y-4">{[["01","Validate","Strictly validate your request and reference evidence."],["02","Pay","Server calculates the current Forge price; the browser cannot choose it."],["03","Review","Paid requests enter human review before infrastructure is used."],["04","Build","Approved requests build in an isolated Android runner."],["05","Verify","The APK is inspected before delivery."]].map(([n,t,x])=><div key={n} className="flex gap-3 rounded-2xl bg-slate-50 p-3"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-lg bg-white text-xs font-black text-indigo-700 shadow-sm">{n}</span><div><p className="text-sm font-bold">{t}</p><p className="mt-1 text-xs leading-5 text-slate-500">{x}</p></div></div>)}</div></aside></form>{result&&<section className="mt-6 rounded-3xl border border-emerald-200 bg-white p-5 shadow-sm sm:p-7"><p className="text-xs font-bold uppercase tracking-wider text-emerald-700">{result.status==="PENDING_REVIEW"?"Payment received":"Request created"}</p><h2 className="mt-1 text-xl font-black">{result.status==="PENDING_REVIEW"?"Forge is queued for review":"Secure checkout is ready"}</h2><p className="mt-2 text-sm text-slate-600">Request ID: <span className="font-mono font-semibold">{result.forgeRequestId}</span></p></section>}</div></main>;
}
