# Prompt Workflows

Pro-only guided workflows reuse the existing PromptStudio generation, perspectives, reusable variables, comparison, and one-click launch capabilities.

The first release is intentionally orchestration-only: it adds no new AI generation call and no new quota reservation. A workflow is selected at `/builder?workflow=<id>` and the builder/result surface keeps the sequence visible while the user completes the existing steps.
